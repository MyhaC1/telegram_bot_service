# 🔧 Исправление 404 ошибки в Telegram Mini App

## Проблема
WebApp выдает 404 ошибку при открытии из Telegram.

## Возможные причины и решения

### 1. Репозиторий не создан или URL неправильный

**Проверьте:**
```powershell
git remote -v
```

**Если видите `YOUR_USERNAME` или ошибку:**

#### Вариант A: Репозиторий уже существует
1. Откройте ваш репозиторий на GitHub
2. Скопируйте правильный URL (например: `https://github.com/MyxaC1/telegram_bot_service.git`)
3. Выполните:
```powershell
git remote remove origin
git remote add origin https://github.com/MyxaC1/telegram_bot_service.git
git push -u origin master
```

#### Вариант B: Репозиторий не создан
1. Перейдите на https://github.com/new
2. Repository name: `telegram_bot_service`
3. Visibility: **Public** (обязательно!)
4. НЕ добавляйте README, .gitignore, license
5. Нажмите **Create repository**
6. Скопируйте URL и выполните:
```powershell
git remote add origin https://github.com/ваш_username/telegram_bot_service.git
git branch -M main
git push -u origin main
```

---

### 2. GitHub Pages не настроен

**После успешного push:**

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source**:
   - Branch: выберите `master` (или `main` если переименовали)
   - Folder: `/ (root)`
4. Нажмите **Save**
5. Подождите 2-3 минуты
6. Обновите страницу — вверху должна появиться ссылка на ваш сайт

**Правильный URL будет:**
```
https://ваш_username.github.io/telegram_bot_service/webapp/
```

Например:
```
https://myxac1.github.io/telegram_bot_service/webapp/
```

---

### 3. Неправильный путь к WebApp

GitHub Pages работает по такой схеме:
- Репозиторий: `github.com/username/repo_name`
- GitHub Pages: `username.github.io/repo_name/`

**Ваш WebApp находится в папке `webapp/`, поэтому правильный URL:**
```
https://username.github.io/telegram_bot_service/webapp/
```

⚠️ **ВАЖНО**: Путь `/webapp/` в конце обязателен!

---

### 4. Обновите .env файл

После того как GitHub Pages заработает, обновите `.env`:

```env
WEBAPP_URL=https://myxac1.github.io/telegram_bot_service/webapp/
```

**Замените `myxac1` на ваш GitHub username!**

---

### 5. Проверка в браузере

**Сначала проверьте в браузере:**

1. Откройте URL: `https://ваш_username.github.io/telegram_bot_service/webapp/`
2. Должна открыться форма регистрации
3. Если 404:
   - Проверьте что репозиторий Public
   - Проверьте что GitHub Pages включен
   - Проверьте правильность пути
   - Подождите еще 2-3 минуты (деплой может занять время)

---

### 6. Отладка

**Проверьте статус деплоя:**
1. Откройте репозиторий на GitHub
2. Перейдите во вкладку **Actions**
3. Должна быть зеленая галочка у последнего коммита
4. Если красный крестик — деплой не удался

**Проверьте что файлы загружены:**
1. Откройте репозиторий на GitHub
2. Должна быть папка `webapp/`
3. В ней должны быть `index.html` и `demo.html`

---

## Быстрая проверка (чек-лист)

- [ ] Репозиторий создан на GitHub
- [ ] Репозиторий **Public** (не Private!)
- [ ] Код запушен (`git push`)
- [ ] GitHub Pages включен (Settings → Pages)
- [ ] Branch выбран правильно (`master` или `main`)
- [ ] Подождали 2-3 минуты после включения Pages
- [ ] URL правильный (с `/webapp/` в конце)
- [ ] URL работает в браузере
- [ ] `.env` обновлен с правильным URL
- [ ] Бот перезапущен после изменения `.env`

---

## Тестирование

После исправления:

1. Остановите бота (Ctrl+C если запущен)
2. Обновите `.env` с правильным URL
3. Запустите бота:
```powershell
python test_bot.py
```
4. Откройте @patriot_man_bot в Telegram
5. Отправьте `/start`
6. Нажмите кнопку "📝 Зарегистрироваться"
7. Должна открыться форма (не 404!)

---

## Если все еще не работает

Напишите мне:
1. URL вашего репозитория на GitHub
2. Что показывает команда: `git remote -v`
3. Скриншот ошибки 404
4. Что показывает Settings → Pages на GitHub
