# Telegram Bot Service

Микросервис с двумя Telegram ботами (Manager Bot + Admin Bot) для управления менеджерами и клиентами через API Gateway.

## 🎯 Архитектура

- **Manager Bot** — регистрация менеджеров, добавление клиентов
- **Admin Bot** — одобрение/отклонение регистраций, управление менеджерами
- **API Gateway** — централизованная точка входа (все запросы к Manager Service и Client Search Service идут через Gateway)
- **База данных** — Tortoise ORM (SQLite для dev, PostgreSQL для production)
- **Redis** — FSM states и кэширование

## 🚀 Быстрый старт

### 1. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните реальные значения:

```powershell
copy .env.example .env
```

**Обязательно замените:**
- `BOT_TOKEN_MANAGER` — токен Manager бота от @BotFather
- `BOT_TOKEN_ADMIN` — токен Admin бота от @BotFather
- `ADMIN_TELEGRAM_IDS` — ваш Telegram ID (можно узнать у @userinfobot)

### 2. Локальный запуск (без Docker)

```powershell
# Создать виртуальное окружение
python -m venv .venv

# Активировать (Windows PowerShell)
.\.venv\Scripts\Activate.ps1

# Установить зависимости
pip install -r requirements.txt

# Запустить сервис
uvicorn app.main:app --reload --port 8002
```

**Важно:** Убедитесь, что Redis запущен локально на порту 6379.

### 3. Запуск в Docker (development)

```powershell
docker compose -f docker-compose.dev.yml up --build
```

### 4. Запуск в Docker (production)

```powershell
# Создать external сеть (если еще не создана)
docker network create microservices_network

# Запустить сервисы
docker compose up --build -d
```

## 📁 Структура проекта

```
telegram_bot_service/
├── app/
│   ├── main.py                          # FastAPI приложение + запуск ботов
│   ├── bots/
│   │   ├── manager_bot/                 # Manager Bot
│   │   │   ├── bot.py
│   │   │   ├── handlers/                # Обработчики команд
│   │   │   ├── keyboards/               # Клавиатуры
│   │   │   ├── states/                  # FSM States
│   │   │   └── middlewares/             # Middleware (auth)
│   │   └── admin_bot/                   # Admin Bot
│   │       ├── bot.py
│   │       ├── handlers/
│   │       ├── keyboards/
│   │       └── middlewares/
│   ├── core/
│   │   ├── config.py                    # Настройки (pydantic)
│   │   ├── database.py                  # Tortoise ORM init
│   │   └── exceptions.py
│   ├── db/
│   │   └── models.py                    # PendingRegistration, BotSession
│   ├── services/
│   │   └── api_gateway_client.py       # HTTP клиент для API Gateway
│   └── utils/
│       ├── logging.py                   # JSON логирование
│       └── validators.py                # Валидаторы email/phone
├── Dockerfile
├── docker-compose.yml                   # Production (Postgres + Redis)
├── docker-compose.dev.yml               # Development (SQLite + Redis)
├── requirements.txt
├── .env.example
└── README.md
```

## 🤖 Использование

### Manager Bot

**Регистрация нового менеджера:**
1. `/start` — начало работы
2. `/register` — запуск регистрации
3. Бот запросит: ФИО → Email → Телефон → Роль
4. Подтверждение → Заявка отправлена админу

**После одобрения админом:**
- Добавление клиентов
- Просмотр своих клиентов
- Статистика

### Admin Bot

**Просмотр и обработка заявок:**
1. `/pending` — список ожидающих регистраций
2. `approve <id>` — одобрить заявку (создаёт менеджера через API Gateway)
3. `reject <id> причина` — отклонить заявку

**Пример:**
```
approve 5
reject 7 неполные данные
```

## 🔗 Интеграция с API Gateway

Все запросы к внешним сервисам идут через API Gateway:

- `POST /api/managers/` — создание менеджера
- `GET /api/managers/{telegram_id}` — получение данных менеджера
- `PATCH /api/managers/{telegram_id}` — обновление менеджера
- `POST /api/clients/` — создание клиента

**Настройка:** измените `API_GATEWAY_URL` в `.env`

## 📊 Health Check

```powershell
curl http://localhost:8002/health
```

Ответ:
```json
{"status": "ok", "service": "telegram_bot_service"}
```

## 🧪 Тестирование

```powershell
# TODO: Добавить тесты с pytest
pytest tests/
```

## 🛠 Разработка

### База данных (миграции)

```powershell
# Инициализация aerich
aerich init -t app.core.database.TORTOISE_ORM

# Создание миграции
aerich migrate

# Применение миграций
aerich upgrade
```

### Логи

Логи структурированы в JSON формате (python-json-logger):

```json
{
  "asctime": "2025-11-18 10:30:45",
  "levelname": "INFO",
  "name": "telegram_bot_service",
  "message": "Manager registration created"
}
```

## 📝 TODO

- [ ] Добавить клавиатуры с кнопками (InlineKeyboard)
- [ ] Реализовать добавление клиентов через Manager Bot
- [ ] Добавить кэширование в Redis (manager:{telegram_id})
- [ ] Написать тесты (pytest + pytest-asyncio)
- [ ] Добавить Webhook поддержку
- [ ] Rate limiting (анти-спам)
- [ ] Prometheus метрики

## 🐛 Troubleshooting

**Ошибка: "Bot token is invalid"**
- Проверьте токены в `.env`
- Убедитесь, что токены получены от @BotFather

**Ошибка: "Redis connection failed"**
- Убедитесь, что Redis запущен: `docker ps` или локально `redis-server`

**Ошибка: "API Gateway unreachable"**
- Проверьте, что API Gateway запущен
- Проверьте `API_GATEWAY_URL` в `.env`
- Убедитесь, что все сервисы в одной Docker сети

## 📄 Лицензия

MIT
