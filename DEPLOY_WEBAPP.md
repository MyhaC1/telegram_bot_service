# Деплой Telegram Mini App на GitHub Pages

## 📋 Инструкция по развертыванию

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название: `telegram_bot_service` (или любое другое)
3. Visibility: **Public** (обязательно для GitHub Pages)
4. Создайте репозиторий

### 2. Загрузите файлы в репозиторий

```powershell
cd C:\Users\user\Desktop\telegram_bot_service

# Инициализируйте git (если еще не сделано)
git init

# Добавьте .gitignore
git add .gitignore

# Добавьте WebApp файлы
git add webapp/

# Также можете добавить весь проект
git add .

# Сделайте коммит
git commit -m "Add Telegram Mini App for manager registration"

# Подключите удаленный репозиторий (замените на ваш URL)
git remote add origin https://github.com/YOUR_GITHUB_USERNAME/telegram_bot_service.git

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

### 3. Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Branch: `main`
   - Folder: `/root` или `/webapp` (если хотите только WebApp)
4. Нажмите **Save**
5. Подождите 2-3 минуты

### 4. Проверьте URL вашего WebApp

После развертывания ваш WebApp будет доступен по адресу:

```
https://YOUR_GITHUB_USERNAME.github.io/telegram_bot_service/webapp/
```

Пример:
```
https://ivanov123.github.io/telegram_bot_service/webapp/
```

### 5. Обновите конфигурацию бота

Откройте `.env` и измените:

```env
WEBAPP_URL=https://YOUR_GITHUB_USERNAME.github.io/telegram_bot_service/webapp/
```

**Важно**: Замените `YOUR_GITHUB_USERNAME` на ваше имя пользователя GitHub!

### 6. Настройте бота в BotFather

1. Откройте @BotFather в Telegram
2. Выберите вашего бота (Manager Bot)
3. Отправьте команду `/setmenubutton`
4. Выберите вашего бота
5. Введите текст кнопки: `Регистрация`
6. Введите URL: `https://YOUR_GITHUB_USERNAME.github.io/telegram_bot_service/webapp/`

Теперь при открытии бота у пользователей будет кнопка Menu с вашим WebApp!

### 7. Перезапустите бота

```powershell
python test_bot.py
```

---

## 🧪 Тестирование WebApp

### Локальное тестирование (без GitHub Pages)

Для быстрого тестирования можно использовать локальный сервер:

```powershell
# Запустите простой HTTP сервер
cd C:\Users\user\Desktop\telegram_bot_service\webapp
python -m http.server 8080
```

Откройте в браузере: http://localhost:8080

**Важно**: Telegram WebApp API будет работать только внутри Telegram!

### Тестирование с ngrok (для разработки)

1. Скачайте ngrok: https://ngrok.com/download
2. Запустите:
```powershell
ngrok http 8080
```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Используйте этот URL в `.env`:
```env
WEBAPP_URL=https://abc123.ngrok.io/
```

---

## 📱 Как использовать

1. Пользователь открывает бота и видит кнопку "📝 Зарегистрироваться"
2. Нажимает на кнопку → открывается WebApp внутри Telegram
3. Заполняет форму (ФИО, Email, Телефон, Роль)
4. Нажимает "Отправить заявку" → данные отправляются боту
5. Бот создает PendingRegistration в БД
6. Админ видит заявку в Admin Bot и одобряет/отклоняет

---

## 🎨 Кастомизация WebApp

Файл `webapp/index.html` можно редактировать:

- **Стили**: изменить CSS для своего дизайна
- **Поля**: добавить дополнительные поля в форму
- **Валидация**: настроить правила валидации
- **Язык**: перевести на другой язык

После изменений:
```powershell
git add webapp/index.html
git commit -m "Update WebApp design"
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

---

## 🔒 Безопасность

WebApp использует официальный Telegram SDK, который:
- ✅ Передает `telegram_id` и `username` пользователя
- ✅ Подписывает данные (можно проверить на стороне бота)
- ✅ Работает только внутри Telegram (защита от спама)

Для дополнительной проверки можно добавить проверку `initData` на стороне бота.

---

## 🐛 Troubleshooting

**WebApp не открывается**
- Проверьте что URL доступен в браузере
- Убедитесь что используется HTTPS (HTTP не работает в Telegram)
- Проверьте что репозиторий Public

**Данные не приходят боту**
- Проверьте логи бота (должен быть handler для `F.web_app_data`)
- Убедитесь что бот запущен
- Проверьте что в WebApp правильно вызывается `tg.sendData()`

**Кнопка не появляется в боте**
- Обновите `.env` с правильным `WEBAPP_URL`
- Перезапустите бота
- Попробуйте `/start` заново
